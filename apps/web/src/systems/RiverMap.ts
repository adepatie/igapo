/**
 * RiverMap — superseded by the scrolling river view in MapScene.
 *
 * The overhead click-map has been replaced with a forward-facing parallax
 * river view. Navigation is now handled via the side panel in MapScene,
 * and world-state display is driven by derivedState in GameState.
 *
 * This file is retained as a placeholder and is not imported anywhere.
 */
export {};
